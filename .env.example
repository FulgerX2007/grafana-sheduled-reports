# Authentication: Plugin uses Grafana managed service accounts (Grafana 10.3+)
# No manual token configuration required

# SMTP Configuration (optional - can also use Grafana's SMTP settings)
GF_SMTP_HOST=smtp.example.com:25
GF_SMTP_USER=smtp_username
GF_SMTP_PASSWORD=your_smtp_password_here
GF_SMTP_FROM_ADDRESS=noreply@example.com
GF_SMTP_FROM_NAME=Grafana Reports
